/**import { test, expect } from '@playwright/test';

test('Texto de Wikipedia visible', async({ page }) => {
    await page.goto('https:es.wikipedia.org');
    const logo = page.locator('a.mw-wiki-logo');
    await expect(logo).toBeVisible();

});*/

